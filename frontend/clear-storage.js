// Quick fix script to clear localStorage and force browser refresh

console.log("🔧 Clearing localStorage to fix DataUpload error...");

// Clear all localStorage
localStorage.clear();

console.log("✅ localStorage cleared!");
console.log("🔄 Please refresh the page (Ctrl+R or Cmd+R)");
console.log("💡 If error persists, do a hard refresh (Ctrl+Shift+R or Cmd+Shift+R)");
